steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t=us-docker.pkg.dev/$PROJECT_ID/sloaneadler/sloaneadler:$COMMIT_SHA'
      - '-t=us-docker.pkg.dev/$PROJECT_ID/sloaneadler/sloaneadler:latest'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '--all-tags'
      - 'us-docker.pkg.dev/$PROJECT_ID/sloaneadler/sloaneadler'

  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'sloaneadler'
      - '--image=us-docker.pkg.dev/$PROJECT_ID/sloaneadler/sloaneadler:$COMMIT_SHA'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--memory=1Gi'
      - '--cpu=1'
      - '--concurrency=80'
      - '--max-instances=10'
      - '--min-instances=0'
      - '--timeout=300s'
      - '--set-env-vars=NODE_ENV=production,NEXT_PUBLIC_SITE_URL=https://sloaneadler.com'
      - '--set-secrets=SITE_PASSWORD=site-password:latest,CSRF_SECRET=csrf-secret:latest'

options:
  machineType: 'E2_MEDIUM'
  logging: CLOUD_LOGGING_ONLY

images:
  - 'gcr.io/$PROJECT_ID/sloaneadler:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/sloaneadler:latest'
